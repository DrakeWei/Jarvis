{
  "id": "context-assembler-budget-summary",
  "name": "Summarize context assembler budget behavior",
  "user_prompt": "Inspect the context assembler and explain how it manages context budget. Mention dynamic context, tool result compaction, and dropped blocks explicitly.",
  "workspace_mode": "bound",
  "execution_mode": "normal",
  "approval_policy": {
    "mode": "deny_all_shell"
  },
  "time_budget_seconds": 180,
  "tags": ["core", "read_only", "context", "runtime"],
  "expected_checks": [
    {
      "kind": "final_text_contains_all",
      "contains": ["dynamic", "tool", "dropped"]
    },
    {
      "kind": "tool_used_any_of",
      "tool_names": ["read_file", "read_file_range"]
    },
    {
      "kind": "workspace_diff_is_empty"
    }
  ],
  "notes": "Read-only core task focused on context assembly and budget handling."
}
