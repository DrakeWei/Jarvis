{
  "id": "approval-recovery-flow-summary",
  "name": "Summarize approval recovery flow",
  "user_prompt": "Inspect the approval and recovery flow for pending runtime actions. Mention pending runtime context, checkpoint-backed recovery, approval resolution, and idempotency explicitly.",
  "workspace_mode": "bound",
  "execution_mode": "normal",
  "approval_policy": {
    "mode": "deny_all_shell"
  },
  "time_budget_seconds": 180,
  "tags": ["core", "read_only", "approval", "recovery", "runtime"],
  "expected_checks": [
    {
      "kind": "final_text_contains_all",
      "contains": ["pending", "checkpoint", "approval", "idempotency"]
    },
    {
      "kind": "tool_used_any_of",
      "tool_names": ["read_file", "read_file_range"]
    },
    {
      "kind": "workspace_diff_is_empty"
    }
  ],
  "notes": "Read-only core task spanning approval_service and runtime recovery behavior."
}
